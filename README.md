# `point_set`

This library contains `struct PointSet`. It is an attempt to implement a highly efficient set representation for (x, y) coordinates, built upon a [`BitArray`](https://github.com/gjf2a/bits).
